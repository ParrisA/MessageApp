<!DOCTYPE html>
<html lang="en">
  <head>
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  </head>
  <img class="giant-background-image" src="assets/colorful-triangles-background_yB0qTG6.jpg" alt="Grey Background">
  <p id="notice"><%= notice %></p>
  <nav class="navbar navbar-fixed-top navbar-transparent">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Parris Messages</a>
        <ul class="nav navbar-nav">
          <% if signed_in? %>
            <li>
            <%= link_to "Sign out", destroy_user_session_path, method: :delete%>
            </li>
            <li>
            <%= link_to 'Delete Account', @user, confirm: 'Are you sure?', method: :delete %>
            </li>
          <%else%>
            <li>
            <%= link_to "Register", new_user_registration_path, method: :get%>
            </li>
            <li> 
            <%= link_to "Login", new_user_session_path%>
            </li>
          <%end%>
        </ul>
      </div>
  </nav>
  <table>
    <thead>
      <tr>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @statics.each do |static| %>
        <tr>
          <td><%= link_to 'Show', static %></td>
          <td><%= link_to 'Edit', edit_static_path(static) %></td>
          <td><%= link_to 'Destroy', static, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>
<style type = "text/css">
  .header{
    font-family: "Raleway", sans-serif;
    font-weight: 200;
  }
  .material-design-nav{
    background-color: white;
    box-shadow: 2px 2px 2px 2px rgba(232, 232, 232, 0.55);
  }

  .giant-background-image{
    width: 100%;
    height: auto;
    position: absolute;
    top: 0;
    left: 0;}
  .navbar-inner {
    background:transparent;
  }
  a:link { 
  color: #FFFFFF;
  }
  a:visited { 
    color: #FFFFFF;
  }
  a:hover { 
    color: #CCCCCC;
  }
  a:active { 
    color: #333333;
  }
</style>
</html>